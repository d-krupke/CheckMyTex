# Streamlit App Dependencies

# Core framework
streamlit>=1.28.0
pandas>=2.0.0

# CheckMyTex library (install from parent directory)
# Install with: pip install -e ..

# Additional dependencies (if not already installed via CheckMyTex)
# These are typically already installed with CheckMyTex
pyspellchecker>=0.7.2
flachtex>=0.3.13
yalafi>=1.3.0
proselint>=0.13.0
rich>=12.5.1
