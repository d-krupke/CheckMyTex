# Docker Compose Environment Configuration
# Copy this to .env for local development: cp .env.example .env

# Port to expose on host machine
# Local: use 80 or 8080
# Production: usually 80 (nginx handles SSL termination)
HTTP_PORT=80
